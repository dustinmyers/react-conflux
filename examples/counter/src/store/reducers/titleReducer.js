const initialState = {
  title: 'This is the title from the titleReducer'
};

export const titleReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
